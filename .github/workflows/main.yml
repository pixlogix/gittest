on:
    push:
        branches:
        - master
name: 🚀 Deploy website on push
jobs:
    web-deploy:
        name: 🎉 Deploy
        runs-on: ubuntu-latest
        steps:
        - name: 🚚 Get latest code
          uses: actions/checkout@v2
        
        - name: 📂 Sync files
          uses: SamKirkland/FTP-Deploy-Action@4.1.0
          with:
            server: 64.4.160.99
            username: admin_projects
            password: ${{ secrets.PROJECTS_FTP_PASSWORD }}
            server-dir: /public_html/wordpress/test_git/